.Rproj.user
.Rhistory
.RData
.Ruserdata
*.Rproj
.DS_Store
data/
demo/
pbmc3k/
.claude/
CLAUDE.md
examples/*/project*/
examples/**/*.rds
inst/doc
docs/

# Built artifacts: package tarballs and deploy bundles are regenerable, often large,
# and must never be committed (*.tar.gz was already in .Rbuildignore but not here).
*.tar.gz
*.zip

# Local dev/scratch scripts (dataset-specific; not part of the package)
dev/

# Internal usage guide — lab reference, not part of the package
USAGE.md

# stray analysis CSV exports copied into the repo for debugging (not package files)
/scroll_de.csv
/scroll_pseudobulk (1).csv
