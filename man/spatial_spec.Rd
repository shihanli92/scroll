% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{spatial_spec}
\alias{spatial_spec}
\title{Describe a dataset's spatial image + coordinates for \code{scroll_build()}}
\usage{
spatial_spec(image = NULL, name = "spatial", image_asset = TRUE)
}
\arguments{
\item{image}{Name of the image / FOV to export (an entry of
\code{SeuratObject::Images(object)}). \code{NULL} (default) uses the first one.}

\item{name}{Embedding name to give the exported coordinates (default
\code{"spatial"}). Appears as a reduction in DimPlot/FeaturePlot and is the
Spatial panel's coordinate system.}

\item{image_asset}{Whether to bake the tissue image as an underlay asset
(default \code{TRUE}). \code{FALSE} exports coordinates only (the Spatial panel then
draws points on a blank background).}
}
\value{
A \code{scroll_spatial_spec} list.
}
\description{
Names the tissue image (Seurat FOV / \verb{@images} slot) whose spot coordinates and
H&E array should be exported. Pass the result as
\code{scroll_build(..., spatial = spatial_spec(...))} to add a \code{spatial} embedding
(from \code{GetTissueCoordinates()}), bake the tissue image, and enable the Spatial
panel. Defaults auto-detect the first image, so a standard Visium object needs
only \code{spatial_spec()}.
}
\seealso{
\code{\link[=scroll_build]{scroll_build()}}
}
