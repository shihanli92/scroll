% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/views.R
\name{view_heatmap}
\alias{view_heatmap}
\title{Expression heatmap across groups (or subsampled cells)}
\usage{
view_heatmap(cells, params, expr_long, state = list(), assembly = NULL)
}
\arguments{
\item{cells}{The cells data.frame (its group sizes are the denominators).}

\item{params}{List with \code{features}, \code{group_by}, and optionally \code{assay}.}

\item{expr_long}{A data.frame(feature, cell, value) in normalized units.}

\item{state}{Optional toggle state (\code{mode}, \code{stat}, \code{scale}, \code{clip}, \code{cluster},
\code{palette}, \code{cell_cap}, \code{aspect}, \code{theme}).}

\item{assembly}{Optional precomputed assembly (as the Shiny app supplies).}
}
\value{
A ggplot, or an aplot composite when dendrograms are attached.
}
\description{
A tile heatmap of mean (or \% expressing) expression per gene per group, with
optional per-gene z-scoring, row/column clustering + dendrograms, and a
diverging palette. A \code{cells} mode instead draws genes x a random subsample of
cells (capped, grouped) via \code{geom_raster}. \code{group_by} may name >1 column (its
\code{a | b} interaction).
}
