% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel-builder.R
\name{register_plot_panel}
\alias{register_plot_panel}
\title{Register a plot panel declaratively (no Shiny boilerplate)}
\usage{
register_plot_panel(
  id,
  plot,
  controls = list(),
  label = id,
  title = label,
  desc = NULL,
  after = NULL,
  before = NULL,
  compute = TRUE,
  csv = FALSE
)
}
\arguments{
\item{id}{Panel id (a single name).}

\item{plot}{\verb{function(cells, input, data)} returning a ggplot. \code{cells} is the
active (subset/view-filtered) cell table; \code{input} exposes each control by its
id; \code{data} is the handle (\code{data$query1}, \code{data$con}, \code{data$manifest}, …).}

\item{controls}{A list of \verb{scroll_input_*()} specs (see \link{scroll_input}).}

\item{label, title, desc, after, before}{As in \code{\link[=register_panel]{register_panel()}}.}

\item{compute}{If \code{TRUE} (default), the plot recomputes on a Compute button;
\code{FALSE} makes it live (recompute on any control change).}

\item{csv}{If \code{TRUE}, add a CSV button that exports the plot's source data. The
\code{plot} function opts in by attaching the table to its result, e.g.
\verb{attr(p, "scroll_source") <- df; p}. Default \code{FALSE}.}
}
\value{
Invisibly, \code{id}.
}
\description{
A higher-level companion to \code{\link[=register_panel]{register_panel()}}: describe the controls and a
single plot function, and scroll generates the whole module — UI, control
population, error surfacing, a Compute gate, downloads, spinner, and app-bar
subset/view awareness. The silent-failure footguns of a hand-written server
(a stray \code{req()}, an uncaught error) are handled for you.
}
\examples{
\dontrun{
register_plot_panel("counts",
  controls = list(scroll_input_column("grp", "Group", "categorical")),
  plot = function(cells, input, data) {
    ggplot2::ggplot(cells, ggplot2::aes(.data[[input$grp]])) + ggplot2::geom_bar()
  },
  label = "Counts", compute = FALSE)
}
}
