% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/views.R
\name{view_feature_multi}
\alias{view_feature_multi}
\title{Embedding faceted into one feature plot per gene (Seurat multi-feature style)}
\usage{
view_feature_multi(cells, params, values_long, state = list())
}
\arguments{
\item{cells}{The globally-loaded cells data.frame.}

\item{params}{List with \code{embedding} and \code{features} (a character vector) and
optionally \code{ncol} (grid columns; default a square-ish layout).}

\item{values_long}{A data.frame(feature, cell, value) for the genes, or \code{NULL}.}

\item{state}{Optional toggle state, passed to each panel.}
}
\value{
A patchwork of feature plots (or the single panel if patchwork is
unavailable / only one gene is given).
}
\description{
Draws one \code{\link{view_feature_plot}} per gene and composes them into a
grid. Each panel gets its own colour scale (a gene's own expression range), as
Seurat's \code{FeaturePlot(features = c(...))} does, so a low-expressed gene is
not washed out beside a high one.
}
