% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{scroll_query_cells}
\alias{scroll_query_cells}
\title{Query all features for a set of cells}
\usage{
scroll_query_cells(con, assay, cells, dict = FALSE)
}
\arguments{
\item{con}{A handle from \code{\link[=scroll_connect]{scroll_connect()}}.}

\item{assay}{Assay name.}

\item{cells}{Cell keys to fetch: the int32 global row index into
\code{cells.parquet} for a v2 store (i.e. \code{cells$.gidx}), or barcodes for a v1 store.}

\item{dict}{If \code{TRUE}, return the \code{feature} column dictionary-encoded (an R
factor) instead of a character vector. arrow builds the dictionary during the
scan, so downstream \code{unique()}/\code{match()} over the tens of millions of repeated
gene names become cheap integer ops — a large speedup when reconstructing a
DE contrast's matrix. The factor's levels are the store's own feature names.}
}
\value{
A data.frame with columns \code{feature}, \code{cell}, \code{value}.
}
\description{
Returns the long (feature, cell, value) records for the given cells across
every feature — used to reconstruct a contrast's expression matrix for live
DE. Unlike a feature lookup this scans all partitions (the cell filter does
not prune the feature-partitioned store), so it is the one query that touches
the whole assay; call it on demand, not per interaction.
}
\section{Deprecated}{

This is an internal step of \code{\link[=scroll_de]{scroll_de()}} and will no longer be exported from
scroll 0.3.0.
}

\keyword{internal}
