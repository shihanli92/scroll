% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/views.R
\name{view_contrast_preview}
\alias{view_contrast_preview}
\title{Live DE contrast preview: cells coloured by contrast membership}
\usage{
view_contrast_preview(cells, embedding, labels, cap = 500L)
}
\arguments{
\item{cells}{The cells data.frame (must carry the embedding's \verb{_1}/\verb{_2} coords).}

\item{embedding}{Reduction name; coordinates read from \verb{<embedding>_1}/\verb{_2}.}

\item{labels}{Per-cell contrast label ("group1" / "group2" / "rest" / \code{NA}), as
produced by the DE panel from the current picks.}

\item{cap}{Max coloured cells drawn per group (stratified sample, for speed).}
}
\value{
A ggplot.
}
\description{
A compact mini-embedding for the DE panel: the whole embedding as a faint grey
outline, with \code{ident1} cells \strong{red} and \code{ident2} (or "rest") cells \strong{blue} on
top. Pure metadata + coordinates (no store query), so it can update live as the
contrast controls change, showing exactly the cells \code{scroll_de()} would test.
}
