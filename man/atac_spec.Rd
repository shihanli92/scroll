% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atac.R
\name{atac_spec}
\alias{atac_spec}
\title{Describe a dataset's scATAC peaks assay for \code{scroll_build()}}
\usage{
atac_spec(assay = "peaks", nearest_gene = TRUE)
}
\arguments{
\item{assay}{Name of the peaks assay (default \code{"peaks"}). Must be one of the
exported \code{assays}.}

\item{nearest_gene}{Whether to compute each peak's nearest gene via
\code{\link[Signac:ClosestFeature]{Signac::ClosestFeature()}} when the assay carries an annotation (default
\code{TRUE}). Falls back to coordinates-only if unavailable.}
}
\value{
A \code{scroll_atac_spec} list.
}
\description{
Marks which exported assay holds chromatin-accessibility \strong{peaks} and bakes a
peak-annotation table. Pass the result as \code{scroll_build(..., atac = atac_spec())}.
The peaks assay must still be listed in \verb{assays=} so its accessibility matrix is
exported like any other assay; \code{atac_spec()} additionally records genomic
coordinates (parsed from the peak names, e.g. \code{chr1-9776-10668}) and, when a
Signac \code{ChromatinAssay} annotation is present, each peak's nearest gene.
}
\seealso{
\code{\link[=scroll_build]{scroll_build()}}
}
