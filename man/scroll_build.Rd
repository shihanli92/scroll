% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build.R
\name{scroll_build}
\alias{scroll_build}
\title{Build a scroll project from a Seurat object}
\usage{
scroll_build(
  object,
  outdir,
  assays = NULL,
  embeddings = NULL,
  meta_cols = NULL,
  quantize = TRUE,
  counts = FALSE,
  subsets = NULL,
  vdj = NULL,
  spatial = NULL,
  atac = NULL,
  overwrite = FALSE,
  verbose = interactive()
)
}
\arguments{
\item{object}{A processed Seurat object, or a path to an \code{.rds} file.}

\item{outdir}{Output project directory (created if missing).}

\item{assays}{Assays to export. \code{NULL} exports the default assay only.}

\item{embeddings}{Reductions to export. \code{NULL} exports all reductions.}

\item{meta_cols}{Metadata columns to export. \code{NULL} infers a sensible set.}

\item{quantize}{If \code{TRUE}, expression is quantized to \code{uint8} (256 levels),
with a per-assay \code{max} recorded in the manifest for dequantization. Values
below ~\code{max/510} round to zero, so fraction-expressing statistics (dotplot
dot size, DE \code{pct.1}/\code{pct.2}) slightly under-count low-expression cells; use
\code{quantize = FALSE} for exact values.}

\item{counts}{If \code{TRUE}, also export each assay's raw \code{counts} layer to a
\verb{counts/<assay>.parquet} store (integer, unquantized). This roughly doubles
expression storage but enables pseudobulk differential expression
(\code{\link[=scroll_pseudobulk_de]{scroll_pseudobulk_de()}}, which needs counts for edgeR/limma-voom).}

\item{subsets}{Optional named list declaring reprocessed \strong{subset views} — a
slice of the data re-embedded in isolation (e.g. a T-cell-only UMAP) plus any
subset-only metadata (subclusters). Each element is \code{list(label =, embeddings =, meta =)}: \code{embeddings} names one or more exported reductions that cover only
the subset's cells (the first is the view's primary embedding; membership =
cells with non-\code{NA} coordinates in it); \code{meta} names metadata columns that are
meaningful only within the subset. In the app a subset becomes a \strong{View} that
restricts every panel to those cells and exposes its embedding + metadata.
Defaults to any spec attached by \code{\link[=scroll_add_subset]{scroll_add_subset()}}.}

\item{vdj}{Optional \code{\link[=vdj_spec]{vdj_spec()}} describing per-cell TCR/BCR columns. When
supplied, a compact \verb{repertoire/} store (clone table + diversity / gene-usage
residuals / tissue correlation) is baked and the VDJ panels are enabled.}

\item{spatial}{Optional \code{\link[=spatial_spec]{spatial_spec()}} (or \code{TRUE} for defaults) describing a
tissue image. When supplied, \code{GetTissueCoordinates()} is exported as a
\code{spatial} embedding, the H&E image is baked as a raster asset, and the Spatial
panel is enabled.}

\item{atac}{Optional \code{\link[=atac_spec]{atac_spec()}} (or \code{TRUE} for defaults) naming a peaks assay.
When supplied, the assay is marked \code{kind: peaks}, each peak's coordinates are
parsed from its name, a peak-annotation table (with nearest gene when a Signac
annotation is available) is baked, and the Peaks panel is enabled.}

\item{overwrite}{If \code{TRUE}, an existing \code{outdir} is removed first.}

\item{verbose}{If \code{TRUE}, report progress: a step message per phase and a
progress bar over each assay's feature-partition export (the slow step).
Defaults to \code{\link[=interactive]{interactive()}}, so it is quiet in scripts/CI; pass \code{TRUE} to
force progress in a non-interactive session.}
}
\value{
\code{outdir}, invisibly.
}
\description{
Reads a processed Seurat object exactly once and emits a self-contained
project directory of lightweight on-disk artifacts: a small Parquet table of
cell metadata + embeddings (\code{cells.parquet}), a feature-partitioned Parquet
store of expression (\verb{expr/<assay>/}), a \code{manifest.yaml}, and authored
scaffolding (\code{config.yaml}, \code{story.qmd}). The running app never touches the
object again.
}
\details{
This is the Phase 1 walking skeleton: it wires a single assay end to end.
}
