% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app.R
\name{scroll_multi_app}
\alias{scroll_multi_app}
\title{Build a multi-dataset scroll explorer}
\usage{
scroll_multi_app(projects)
}
\arguments{
\item{projects}{A named list/vector of built project directories. Names are used
as tab labels (falling back to each project's \code{config.yaml} title, then a
generic label).}
}
\value{
A \code{shiny.appobj}.
}
\description{
Mounts several built scroll projects behind one app: a tab per dataset, each
with its own app bar, manifest-driven panels, and query handle. Every dataset's
UI is namespaced (via \code{\link[shiny:NS]{shiny::NS()}}) so the projects coexist without id
collisions; the built-in and custom panels are the same registry for all.
}
\details{
Custom panels that read baked assets should resolve them from the per-dataset
handle (\code{data$dir}) rather than a global option, so each tab reads its own
project's files.

The \code{config.yaml} panel override (\verb{panels:} / \verb{exclude_panels:}) is honoured by
\code{\link[=scroll_app]{scroll_app()}}; here the tab panel list is shared, so the panels are the gated
union across all mounted projects.
}
