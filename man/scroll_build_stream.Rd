% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build-stream.R
\name{scroll_build_stream}
\alias{scroll_build_stream}
\title{Build a scroll project by streaming many sources (large-dataset build)}
\usage{
scroll_build_stream(
  outdir,
  sources,
  reader,
  assays = NULL,
  embeddings = NULL,
  meta_cols = NULL,
  quantize = FALSE,
  id_of = as.character,
  overwrite = FALSE,
  verbose = interactive()
)
}
\arguments{
\item{outdir}{Project directory to create or append to.}

\item{sources}{A list/vector of sources (file paths, ids, …) passed one at a
time to \code{reader}.}

\item{reader}{\verb{function(source) -> Seurat object}, already processed (a
log-normalized \code{data} layer, the reductions, and the metadata columns).}

\item{assays, embeddings}{Names to export; \code{NULL} takes the default assay / all
reductions of the \strong{first} source (then held constant across sources).}

\item{meta_cols}{Metadata columns to expose (required; the reader's objects may
be heterogeneous, so the columns are declared explicitly).}

\item{quantize}{Must be \code{FALSE} (streaming stores float32; see Details).}

\item{id_of}{\verb{function(source) -> character} id used for append-tracking and
part-file tags (default \code{as.character}).}

\item{overwrite}{If \code{TRUE}, wipe \code{outdir} first (a fresh build).}

\item{verbose}{Print per-source progress.}
}
\value{
Invisibly, \code{outdir}.
}
\description{
Produces the same flat-RAM store as \code{\link[=scroll_build]{scroll_build()}}, but assembled from many
sources one at a time, so peak memory stays ~per-source instead of loading the
whole (possibly multi-million-cell) dataset at once. Each source is read by
\code{reader}, its expression appended to a shared v2 store under a running global
cell index, and the manifest written once at the end. The store is \strong{compacted}
(one part-file per feature) on completion.
}
\details{
Idempotent and \strong{append-safe}: re-running with more \code{sources} adds only the new
ones (tracked in \code{outdir/.stream_sources.txt}), so a build can grow as data
arrives. Values are stored unquantized (\code{float32}); a global quantization max is
unknown while streaming, so \code{quantize = TRUE} is not supported here.
}
\seealso{
\code{\link[=scroll_build]{scroll_build()}}
}
