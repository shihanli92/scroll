% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/views.R
\name{view_violin_multi}
\alias{view_violin_multi}
\title{Violin plots for several genes, one panel per gene}
\usage{
view_violin_multi(cells, params, values_long, state = list())
}
\arguments{
\item{cells}{The cells data.frame.}

\item{params}{List with \code{group_by}, \code{features} (a character vector) and
optionally \code{ncol} (grid columns; default a square-ish layout).}

\item{values_long}{A data.frame(feature, cell, value) for the genes, or \code{NULL}.}

\item{state}{Optional toggle state, passed to each panel.}
}
\value{
A patchwork of violins (or the single panel if patchwork is
unavailable / only one gene is given).
}
\description{
Draws one \code{\link{view_violin}} per gene (each with its own expression
axis) and composes them into a grid — the multi-feature analogue of
\code{Seurat::VlnPlot(features = c(...))}.
}
