% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update.R
\name{scroll_add_meta}
\alias{scroll_add_meta}
\title{Add (or replace) a metadata column in a built project -- no Seurat object needed}
\usage{
scroll_add_meta(
  dir,
  name,
  values,
  scope = NULL,
  overwrite = FALSE,
  max_levels = .SCROLL_MAX_LEVELS
)
}
\arguments{
\item{dir}{A built scroll project directory (with \code{cells.parquet} + \code{manifest.yaml}).}

\item{name}{Name of the column to add. Cannot be \code{"cell"} (the barcode column); an
existing metadata column is replaced only when \code{overwrite = TRUE}.}

\item{values}{The column, one of: a vector of length \code{n_cells} in \code{cells.parquet}
\strong{row order}; a \strong{named} vector keyed by cell barcode (unmatched cells become
\code{NA}); a \code{data.frame} with a \code{cell} column and one value column; or a
\verb{function(cells)} returning one of those (it receives the loaded \code{cells}
data.frame, so you can derive the column from existing columns). Character /
factor / logical values register as categorical, numeric as numeric.}

\item{scope}{Optional name of a subset view (a key of the manifest \code{subsets}); when
set, the column is scoped to that view -- its levels are computed over the view's
members only, so it surfaces only inside that View.}

\item{overwrite}{Replace an existing column of the same name (default \code{FALSE}).}

\item{max_levels}{Cap on cached categorical levels written to the manifest; above it
the level list is omitted and recomputed at runtime (default 200).}
}
\value{
\code{invisible(dir)}.
}
\description{
Writes a new column into a built project's \code{cells.parquet} and registers it in
\code{manifest.yaml}, so it becomes selectable in the app on the next restart
(Color-by / group-by and DotPlot/Violin/Proportions for a \strong{categorical} column;
FeaturePlot / Biaxial for a \strong{numeric} one). Unlike \code{\link[=scroll_update]{scroll_update()}} this needs
only the on-disk project, not the original Seurat object -- handy for editing a
deployed app. The column must be something you can compute from what the project
already holds: other metadata columns, or expression queried with
\code{\link[=scroll_query_feature]{scroll_query_feature()}}. The expression store is untouched.
}
\seealso{
\code{\link[=scroll_update]{scroll_update()}} to add columns/embeddings from a Seurat object.
}
