% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel-builder.R
\name{scroll_helpers}
\alias{scroll_helpers}
\alias{scroll_point_layer}
\alias{scroll_discrete_colors}
\alias{scroll_continuous_scale}
\alias{scroll_group_labels}
\title{Reusable plotting helpers for custom views}
\usage{
scroll_point_layer(mapping = NULL, size = 0.6, alpha = 1, raster = FALSE, ...)

scroll_discrete_colors(values, palette = "Tableau 10")

scroll_continuous_scale(palette = "viridis", name = NULL, limits = NULL)

scroll_group_labels(df, group, x, y)
}
\arguments{
\item{mapping, size, alpha, raster, ...}{For \code{scroll_point_layer()}: a ggplot2
aesthetic mapping, point size, opacity, whether to rasterize on screen, and
extra args passed to the geom.}

\item{values}{A vector of categorical values (its unique levels are coloured).}

\item{palette}{A palette name (discrete: e.g. \code{"Tableau 10"}; continuous: e.g.
\code{"viridis"}, \code{"magma"}, a ColorBrewer name, or \code{"grey-red"}).}

\item{name, limits}{For \code{scroll_continuous_scale()}: legend title and optional
\code{c(low, high)} clip (out-of-range values are squished, not dropped).}

\item{df}{A data.frame with the scatter coordinates and group column.}

\item{group, x, y}{For \code{scroll_group_labels()}: the grouping and coordinate
column names in \code{df}.}
}
\value{
A ggplot2 layer/scale (or, for \code{scroll_discrete_colors}, a named vector).
}
\description{
The pieces the built-in panels use, exposed so a custom \code{plot} (or a raw
\code{\link[=register_panel]{register_panel()}} view) matches their look and performance:
}
\details{
\itemize{
\item \code{scroll_point_layer()} — a scatter layer that rasterizes on screen for speed
(via scattermore, when installed and \code{raster = TRUE}) but stays a true
\code{geom_point} otherwise, so exports and small plots are unchanged.
\item \code{scroll_discrete_colors()} — a named colour vector for a categorical column,
keyed by level so a group keeps its colour across panels.
\item \code{scroll_continuous_scale()} — a ggplot2 continuous colour scale (viridis,
ColorBrewer, or grey-to-hue) matching FeaturePlot.
\item \code{scroll_group_labels()} — per-group median-centroid text labels for a scatter.
}
}
