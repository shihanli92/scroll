% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update.R
\name{scroll_update}
\alias{scroll_update}
\title{Add or update reductions / metadata on a built project (no expr rebuild)}
\usage{
scroll_update(
  dir,
  object,
  embeddings = NULL,
  meta_cols = NULL,
  subsets = NULL,
  verbose = interactive()
)
}
\arguments{
\item{dir}{A built scroll project directory (must contain \code{cells.parquet} and
\code{manifest.yaml}).}

\item{object}{A Seurat object (or path to \code{.rds}) whose barcodes overlap the
project. Only the requested reductions / metadata columns are read from it.}

\item{embeddings}{Character vector of reduction names in \code{object} to add as
embedding coordinate columns (e.g. \code{c("umap_tcell")}). \code{NULL} adds none.}

\item{meta_cols}{Character vector of metadata columns in \code{object} to add /
replace. \code{NULL} adds none.}

\item{subsets}{Optional named list of subset-view specs (as in \code{\link[=scroll_build]{scroll_build()}});
defaults to any spec attached by \code{\link[=scroll_add_subset]{scroll_add_subset()}}. Their embeddings /
meta must be present after this update (existing or newly added).}

\item{verbose}{If \code{TRUE}, report what was written.}
}
\value{
\code{dir}, invisibly.
}
\description{
Rewrites only \code{cells.parquet} and \code{manifest.yaml}, aligning new columns to the
existing cells \strong{by barcode} (cells absent from \code{object} get \code{NA}, i.e. a
partial-coverage subset embedding). The feature-partitioned \verb{expr/} and
\verb{counts/} stores are left byte-identical, so this is seconds rather than a full
\code{\link[=scroll_build]{scroll_build()}} re-export.
}
\details{
The arguments mirror \code{\link[=scroll_build]{scroll_build()}}, so the \emph{same} object you would rebuild
from can instead be passed here to append just the new pieces. Columns that
already exist are overwritten (e.g. to relabel a metadata column); new ones are
added. Subset views are registered exactly as in \code{\link[=scroll_build]{scroll_build()}} /
\code{\link[=scroll_add_subset]{scroll_add_subset()}}.
}
\seealso{
\code{\link[=scroll_build]{scroll_build()}}, \code{\link[=scroll_add_subset]{scroll_add_subset()}}
}
