% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pseudobulk.R
\name{scroll_pseudobulk_stability}
\alias{scroll_pseudobulk_stability}
\title{Stability of pseudobulk DE across random pseudo-replicate draws}
\usage{
scroll_pseudobulk_stability(
  data,
  assay,
  aggregate_cols,
  ident1,
  ident2 = NULL,
  replicate_col = "no_replicate",
  min_cells = 10,
  n_pseudo = 3,
  cells_per_pseudo = 50,
  cells = NULL,
  runs = 25,
  lfc = 1,
  padj = 0.05
)
}
\arguments{
\item{data}{A scroll data handle (\code{con}, \code{cells}, \code{manifest}).}

\item{assay}{Assay to test.}

\item{aggregate_cols}{Metadata columns whose combinations define the groups.}

\item{ident1}{Combined levels forming the group of interest.}

\item{ident2}{Combined levels for the comparison group, or \code{NULL}/\code{"rest"} for
one-vs-rest (all cells not in \code{ident1}, restricted to observed combinations).}

\item{replicate_col}{Metadata column defining biological replicates. \code{NULL} or
\code{"no_replicate"} pseudo-replicates the two groups directly (a disjoint
partition of each group's cells) instead of using a real replicate column.
Cells with a missing replicate value are excluded (and reported) rather than
silently dropped; a named column that is absent is an error.}

\item{min_cells}{Minimum cells for a pseudobulk sample to be kept (a per-sample
floor, applied to real replicates and to each pseudo-partition bin).}

\item{n_pseudo, cells_per_pseudo}{Pseudo-replicate partition: number of
pseudo-replicates and the maximum cells per pseudo-replicate.}

\item{cells}{Cells to test over (default \code{data$cells}).}

\item{runs}{Number of random re-runs.}

\item{lfc, padj}{Stability only (\code{runs > 1}): the logFC and adjusted-p cutoffs
defining a "hit" in each run.}
}
\value{
See \code{\link[=scroll_pseudobulk_de]{scroll_pseudobulk_de()}} (\code{runs > 1}).
}
\description{
Deprecated: use \code{scroll_pseudobulk_de(..., runs = )}, which returns the same
stability table when \code{runs > 1}. This wrapper will be removed in scroll 0.3.0.
}
\keyword{internal}
