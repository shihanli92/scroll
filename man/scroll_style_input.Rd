% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel-builder.R
\name{scroll_style_input}
\alias{scroll_style_input}
\title{Put a control in the plot's Style sheet}
\usage{
scroll_style_input(control_spec)
}
\arguments{
\item{control_spec}{A control spec from a \verb{scroll_input_*()} constructor.}
}
\value{
The decorated control spec.
}
\description{
Decorates a \verb{scroll_input_*()} spec so the builder renders it in the panel's
\strong{Style sheet} (opened from the paintbrush button in the plot toolbar) instead
of the control column. Use it for "how the plot looks" options -- palettes, point
sizes, aspect ratio -- and keep "what to plot" in the column. The control keeps
its id, so the \code{plot} function reads it from \code{input} exactly as before, and it
can be combined with \code{\link[=scroll_show_when]{scroll_show_when()}}.
}
\examples{
\dontrun{
register_plot_panel("my_panel",
  controls = list(
    scroll_input_column("group", "Group by", type = "categorical"),
    scroll_style_input(scroll_input_slider("size", "Point size", 0.1, 3, 1, 0.1))),
  plot = function(cells, input, data) ...)
}
}
