% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel-builder.R
\name{scroll_input}
\alias{scroll_input}
\alias{scroll_input_column}
\alias{scroll_input_levels}
\alias{scroll_input_gene}
\alias{scroll_input_numeric}
\alias{scroll_input_slider}
\alias{scroll_input_choice}
\alias{scroll_input_text}
\alias{scroll_input_palette}
\alias{scroll_input_assay}
\alias{scroll_input_embedding}
\alias{scroll_input_custom}
\title{Declarative controls for \code{\link[=register_plot_panel]{register_plot_panel()}}}
\usage{
scroll_input_column(
  id,
  label,
  type = "categorical",
  selected = 1,
  view_aware = FALSE,
  prefer = NULL
)

scroll_input_levels(
  id,
  label,
  from = NULL,
  choices = NULL,
  watch = NULL,
  multiple = TRUE,
  required = FALSE,
  none = FALSE,
  selected = 1
)

scroll_input_gene(id, label, multiple = FALSE, required = FALSE)

scroll_input_numeric(id, label, value, min = NA, max = NA, step = NA)

scroll_input_slider(id, label, min, max, value, step = NULL)

scroll_input_choice(
  id,
  label,
  choices,
  selected = NULL,
  inline = TRUE,
  multiple = FALSE,
  widget = c("auto", "radio", "select")
)

scroll_input_text(id, label, placeholder = NULL)

scroll_input_palette(
  id,
  label = "Palette",
  type = c("discrete", "continuous"),
  selected = NULL
)

scroll_input_assay(id, label = "Assay")

scroll_input_embedding(id, label = "Embedding", view_aware = TRUE)

scroll_input_custom(id, label, ui, bind = NULL, needs = NULL, required = FALSE)
}
\arguments{
\item{id}{Control id (also the name under which its value reaches \code{plot}'s
\code{input}).}

\item{label}{Control label.}

\item{type}{For \code{scroll_input_column}: \code{"categorical"}, \code{"numeric"}, or \code{"any"};
for \code{scroll_input_palette}: \code{"discrete"} or \code{"continuous"}.}

\item{selected}{Default selection: integer index/vector, or \code{"none"}. For
\code{scroll_input_palette}, a palette name.}

\item{view_aware}{For \code{scroll_input_column}/\code{scroll_input_embedding}: repopulate
choices from the active subset view (requires the panel to be view-aware).}

\item{prefer}{For \code{scroll_input_column}: one or more column names to default-select
when present (first match wins), falling back to \code{selected} otherwise.}

\item{from}{For \code{scroll_input_levels}: a column-picking control's id, or a
categorical column name. Supply either \code{from} or \code{choices}.}

\item{choices}{For most inputs a static vector; \code{scroll_input_choice} also accepts
a \verb{function(data)} and \code{scroll_input_levels} a \verb{function(input, data)}, both
evaluated to derive choices from the data handle (e.g. a baked asset under
\code{data$dir}).}

\item{watch}{For \code{scroll_input_levels} with a \code{choices} function: control ids whose
change repopulates the levels (empty = populate once at start-up).}

\item{multiple}{Allow multiple selections.}

\item{required}{If \code{TRUE}, an empty value shows a "Select \if{html}{\out{<label>}}." message
instead of computing.}

\item{none}{If \code{TRUE}, the level selector defaults to empty (e.g. a "vs. rest").}

\item{value, min, max, step, placeholder, inline}{Passed to the underlying Shiny input.}

\item{widget}{For \code{scroll_input_choice}: \code{"radio"}, \code{"select"}, or \code{"auto"}
(radio for a short static list, a selectize dropdown otherwise).}

\item{ui}{For \code{scroll_input_custom}: a \verb{function(ns, data)} returning the
control's UI (namespace inputs with \code{ns()}).}

\item{bind}{For \code{scroll_input_custom}: an optional
\verb{function(input, session, data)} wiring server-side behaviour.}

\item{needs}{For \code{scroll_input_custom}: a required column type
(\code{"categorical"}/\code{"numeric"}) that gates the panel's empty state, or \code{NULL}.}
}
\value{
A control spec (a list) for \code{register_plot_panel(controls = )}.
}
\description{
Each constructor returns a control spec. \code{scroll_input_column} picks a metadata
column of a type; \code{scroll_input_levels} is a level selector bound to the
column chosen by another control (\code{from}) or a fixed column; \code{scroll_input_gene} is a server-side gene search over the default
assay; \code{scroll_input_assay} / \code{scroll_input_embedding} pick an assay / reduction
from the manifest (the embedding narrows to the active subset view when
\code{view_aware}); \code{scroll_input_custom} wraps your own \code{ui}/\code{bind} as a control; the
rest are thin wrappers over the matching Shiny inputs. Wrap any of them in
\code{\link[=scroll_show_when]{scroll_show_when()}} for conditional visibility.
}
