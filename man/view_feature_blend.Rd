% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/views.R
\name{view_feature_blend}
\alias{view_feature_blend}
\title{Embedding coloured by the co-expression of two features (Seurat-style blend)}
\usage{
view_feature_blend(cells, params, values1, values2, state = list())
}
\arguments{
\item{cells}{The globally-loaded cells data.frame.}

\item{params}{List with \code{embedding}, \code{feature1}, \code{feature2}, and optionally
\code{blend_threshold} (0-1, Seurat's \code{blend.threshold}, default 0.5).}

\item{values1, values2}{data.frame(cell, value) for each feature, or \code{NULL}.}

\item{state}{Optional toggle state (\code{point_size}, \code{order}, \code{raster}, \code{aspect}).}
}
\value{
A patchwork of four ggplots (or, if patchwork is unavailable, the
single co-expression panel).
}
\description{
Reproduces \code{Seurat::FeaturePlot(blend = TRUE)}: each feature is scaled to a
0-9 level, the two are blended through Seurat's colour matrix, and four panels
are laid out — feature 1, feature 2, the co-expression blend, and a 2-D colour
key. Faithful to Seurat but with no Seurat runtime dependency.
}
