% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app.R
\name{register_panel}
\alias{register_panel}
\title{Register a custom panel in the scroll explorer}
\usage{
register_panel(
  id,
  ui,
  server,
  label = id,
  title = label,
  desc = NULL,
  after = NULL,
  before = NULL
)
}
\arguments{
\item{id}{Unique panel id: a letter followed by letters, digits, or
underscores. Used as the section anchor and the Shiny module namespace.}

\item{ui, server}{The panel's UI and server functions (see contract above).}

\item{label}{Short rail label (defaults to \code{id}).}

\item{title, desc}{Section heading and one-line description.}

\item{after}{Id of the panel to insert this one after; \code{NULL} (default)
appends at the end. Ignored when replacing an existing id.}

\item{before}{Id of the panel to insert this one before (e.g. \code{"dimplot"} to
put it at the top). Takes precedence over \code{after}. Ignored when replacing an
existing id.}
}
\value{
Invisibly, \code{id}.
}
\description{
Adds a user-defined analysis panel to every \code{\link[=scroll_app]{scroll_app()}} built afterwards in
this session. A panel follows the same module contract as the built-ins — a
pair of functions:
}
\details{
\itemize{
\item \code{ui(id, data)} returns a UI tag; namespace its inputs with \code{\link[shiny:NS]{shiny::NS()}}\code{(id)}.
\item \code{server(id, data, cells_r)} wires the module, typically via
\code{\link[shiny:moduleServer]{shiny::moduleServer()}}. \code{cells_r()} is a reactive of the \emph{active} cells
(already narrowed by the app-bar subset filter); use it, not \code{data$cells},
so the panel honours the global filter.
}

\code{data} is the shared handle: \code{data$cells} (a data.frame of metadata +
embedding coordinates), \code{data$manifest}, \code{data$config}, and the query helpers
\code{data$query1(assay, feature)} / \code{data$queryN(assay, features)}, which return
dequantized long expression (\code{cell}, \code{value} / \code{feature}, \code{cell}, \code{value}).

Registering an \code{id} that matches an existing panel — built-in or custom —
replaces it in place, so you can override a built-in. Clear all custom panels
with \code{\link[=scroll_reset_panels]{scroll_reset_panels()}}.
}
\examples{
# A minimal custom panel: a cells-per-group bar chart.
count_ui <- function(id, data) {
  ns <- shiny::NS(id)
  cols <- names(Filter(function(x) identical(x$type, "categorical"),
                       data$manifest$meta))
  shiny::tagList(shiny::selectInput(ns("grp"), "Group", cols),
                 shiny::plotOutput(ns("plot")))
}
count_server <- function(id, data, cells_r = shiny::reactive(data$cells)) {
  shiny::moduleServer(id, function(input, output, session) {
    output$plot <- shiny::renderPlot({
      shiny::req(input$grp)
      barplot(table(cells_r()[[input$grp]]))
    })
  })
}
register_panel("counts", count_ui, count_server, label = "Counts",
               title = "Cells per group")
scroll_reset_panels()   # (undo, so the example leaves no state)
}
