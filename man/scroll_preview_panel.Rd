% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app.R
\name{scroll_preview_panel}
\alias{scroll_preview_panel}
\title{Preview a single panel while developing it}
\usage{
scroll_preview_panel(panel, dir = ".")
}
\arguments{
\item{panel}{The id of a panel registered with \code{\link[=register_panel]{register_panel()}} /
\code{\link[=register_plot_panel]{register_plot_panel()}} (or a built-in id, e.g. \code{"dotplot"}).}

\item{dir}{A built scroll project directory to render the panel against.}
}
\value{
A \code{shiny.appobj} — auto-prints/launches at the console, or pass to
\code{\link[shiny:runApp]{shiny::runApp()}}.
}
\description{
Mounts \strong{one} panel in the full scroll harness — the app bar (with the
cell-subset filter and the subset-View selector) but none of the other panels —
so a custom panel can be driven for quick feedback without building or scrolling
through the whole app. Because the harness is the real one, the panel is tested
exactly as it will run: under the app-bar filter and any subset views.
}
\details{
The development loop is:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{source("panels/my_panel.R")               # (re-)registers the panel
scroll_preview_panel("my_panel", "projects/demo")
# edit the panel file -> re-source -> re-run
}\if{html}{\out{</div>}}
}
\seealso{
\code{\link[=register_plot_panel]{register_plot_panel()}}, \code{\link[=scroll_app]{scroll_app()}}
}
