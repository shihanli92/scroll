% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel-builder.R
\name{scroll_render_plot}
\alias{scroll_render_plot}
\title{Wire a plot output with error surfacing and PNG/PDF downloads}
\usage{
scroll_render_plot(
  output,
  id,
  fun,
  event = NULL,
  placeholder = NULL,
  lazy = FALSE,
  input = NULL
)
}
\arguments{
\item{output}{The module's \code{output}.}

\item{id}{The panel id (used for download filenames).}

\item{fun}{A zero-argument function returning a ggplot (may \code{stop()} on bad
input — the message is surfaced).}

\item{event}{\code{NULL} for a live plot, or a reactive gating recomputation (e.g.
\code{shiny::reactive(input$go)} for a Compute button). When it is an action-button
count, the plot waits for the first click.}

\item{placeholder}{Optional message shown before the first \code{event} fires.}

\item{lazy}{When \code{TRUE} (and \code{event} is \code{NULL}), gate a live plot on the panel's
on-screen state, so a scrolled-away panel reuses its last render instead of
redrawing on a global filter / View change. Requires \code{input}.}

\item{input}{The module's \code{input} (only needed when \code{lazy = TRUE}).}
}
\value{
Invisibly, the plot reactive.
}
\description{
One call replaces the usual \code{reactive}/\code{eventReactive} + \code{tryCatch} +
\code{renderPlot} + download-handler boilerplate. \code{fun} is evaluated inside a
\code{tryCatch}; any error (or a non-plot return) is shown \strong{inline} in the plot
area rather than failing silently, and PNG/PDF handlers (\code{output$png} /
\code{output$pdf}, matching \code{\link[=scroll_render_plot]{scroll_render_plot()}}'s companion UI
\code{.scroll_plot_area()}) are registered for the result.
}
